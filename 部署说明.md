# 若依 WMS 本地部署说明

## 已完成

- 已下载并放置前端源码：`ruoyi-wms-frontend`
- 已下载并放置后端源码：`ruoyi-wms-backend`
- 已安装前端依赖：`ruoyi-wms-frontend/node_modules`
- 已启动前端开发服务：`http://localhost/`
- 已将前端开发环境 API 改为本机代理：`VITE_APP_BASE_API = '/dev-api'`
- 后端 SQL 初始化脚本位于：`ruoyi-wms-backend/script/sql/wms.sql`

## 当前阻塞

当前电脑缺少完整后端运行环境，所以后端尚未启动：

- 缺少 `JDK 17` 的 `javac` 编译器，当前只有 `java` 运行时
- 缺少 `Maven`
- 缺少 `MySQL`
- 缺少 `Redis`

前端现在能打开登录页，但验证码接口会失败，因为本机后端 `http://localhost:8080` 尚未启动。

## 后端默认配置

后端配置文件：`ruoyi-wms-backend/ruoyi-admin-wms/src/main/resources/application-dev.yml`

默认数据库连接：

- 数据库：`ry-vue`
- 地址：`localhost:3306`
- 用户名：`root`
- 密码：`root`

默认 Redis：

- 地址：`localhost`
- 端口：`6379`

默认后端端口：

- `8080`

## 补齐依赖后启动

1. 安装并配置 `JDK 17`，确认命令可用：

```bat
javac -version
```

2. 安装并配置 `Maven 3.8+`，确认命令可用：

```bat
mvn -version
```

3. 启动 MySQL，创建数据库并导入 SQL：

```sql
CREATE DATABASE `ry-vue` DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
```

然后导入：

```text
ruoyi-wms-backend/script/sql/wms.sql
```

4. 启动 Redis：

```bat
redis-server
```

5. 启动后端：

```bat
start-backend.bat
```

6. 启动前端：

```bat
start-frontend.bat
```

## 访问地址

- 前端：`http://localhost/`
- 后端：`http://localhost:8080/`

## 说明

项目来源：

- 前端：`https://github.com/zccbbg/RuoYi-WMS-VUE`
- 后端：`https://github.com/zccbbg/wms-ruoyi`

该项目的 `master` 分支对应 JDK17 + Vue3 的轻量版本，后端 README 说明 `master` 同步 `lite` 分支。
