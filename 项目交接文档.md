# WMS 仓储管理系统 - 项目交接文档

> 本文档面向接手开发的新 AI 会话，涵盖项目全貌、硬约束、业务流程、环境配置、踩坑记录和下一步建议。
> 读完本文档即可开始继续开发，无需翻阅历史对话。
>
> 最后更新：2026-08-04 | 当前 HEAD：`a165e4e`

---

## 一、项目全景

### 1.1 这是什么

基于 RuoYi-Vue-Plus 框架二次开发的 WMS（仓储管理系统），对接 RCS 智能集群调度系统，通过 AGV 实现入库、出库、移库、盘点的自动化作业。前端 PC 管理后台 + 移动端 Web 扫码 + Android App 三端覆盖。

### 1.2 技术栈

| 层 | 技术 | 版本/说明 |
|----|------|-----------|
| 后端框架 | Spring Boot 3 + RuoYi-Vue-Plus | JDK 17, Maven |
| ORM | MyBatis-Plus | 雪花算法主键, 逻辑删除 |
| 数据库 | MariaDB / MySQL | 数据库名 `ry-vue`, 端口 3306 |
| 缓存 | Redis | 端口 6379, database 0 |
| 认证 | Sa-Token | JWT, Bearer token, 有效期 86400s |
| 前端框架 | Vue 3 + Element Plus + Vite | 端口 80 |
| 移动端 | 独立 HTML 页面 `wms-mobile.html` | 端口 8000 |
| 安卓 App | Capacitor 6 包装移动端网页 | 输出 `wms-mobile.apk` |
| 版本控制 | Git (mingit) | 本地仓库, 无远程 |

### 1.3 目录结构

```
项目根/
├── ruoyi-wms-backend/          # 后端 Maven 项目
│   ├── ruoyi-admin-wms/        # 主启动模块 (含 WMS 业务代码)
│   │   └── src/main/java/com/ruoyi/wms/
│   │       ├── controller/     # 23 个 Controller
│   │       ├── service/        # 业务逻辑层
│   │       ├── mapper/         # MyBatis Mapper
│   │       ├── domain/         # 实体类 (entity/vo/bo)
│   │       ├── rcs/            # RCS 对接相关类
│   │       ├── handler/        # 处理器
│   │       └── task/           # 定时任务
│   ├── ruoyi-common/           # 公共模块 (含 ratelimiter 等)
│   ├── ruoyi-modules/          # 系统模块 (ruoyi-system 等)
│   └── script/sql/             # SQL 脚本
│       ├── wms.sql             # WMS 核心表结构 + 初始数据
│       ├── database_menu.sql   # 数据库初始化菜单
│       ├── inventory_snapshot.sql
│       ├── stock_warning.sql
│       └── wave_pick.sql
├── ruoyi-wms-frontend/         # 前端 Vue 3 项目
│   ├── src/
│   │   ├── views/wms/          # WMS 业务页面 (order/inventory/...)
│   │   ├── views/system/       # 系统管理页面 (database/location)
│   │   ├── views/dashboard/    # 仪表盘
│   │   ├── api/wms/            # WMS API 调用
│   │   └── router/index.js     # 路由配置
│   ├── vite.config.js          # Vite 配置 (代理到 localhost:8080)
│   ├── .env.development        # 开发环境 (VITE_APP_BASE_API = '/dev-api')
│   └── .env.production         # 生产环境 (VITE_APP_BASE_API = '/prod-api')
├── wms-mobile.html             # 移动端 Web 页面 (端口 8000 独立服务)
├── wms-mobile.apk              # 安卓 App 安装包
├── WmsPdaApp/                  # Capacitor 安卓工程
├── docker/                     # Docker 配置
├── .tools/mingit/              # 内置 Git (cmd/git.exe)
├── git管理.bat                  # Git 版本管理工具 (图形化)
├── 会话总结.md                  # 上一次会话工作总结
├── 部署说明.md                  # 本地部署说明
└── 项目交接文档.md              # 本文档
```

### 1.4 端口与服务

| 服务 | 端口 | 说明 |
|------|------|------|
| 后端 API | 8080 | Spring Boot, 上下文路径 `/` |
| 前端 PC | 80 | Vite dev server, 代理 `/dev-api` → `localhost:8080` |
| 移动端 Web | 8000 | 独立 HTTP 服务, 提供移动端扫码页面 |
| MariaDB | 3306 | 数据库 `ry-vue`, root/root |
| Redis | 6379 | database 0, 无密码 |
| RCS 调度系统 | 9001 | `http://192.168.1.36:9001` (局域网 AGV 调度) |

端口固定不可更改。前端 Vite 代理配置在 `ruoyi-wms-frontend/vite.config.js`。

---

## 二、环境配置与启动

### 2.1 后端依赖

后端运行需要以下环境（缺一不可）：

- **JDK 17**：需要 `javac` 编译器（不仅是 JRE），用于 Maven 编译
- **Maven**：用于编译打包，命令 `mvn clean package -DskipTests`
- **MariaDB / MySQL**：数据库 `ry-vue`，需先导入 `script/sql/wms.sql`
- **Redis**：缓存和分布式锁

### 2.2 后端启动

```powershell
# 1. 编译（在 ruoyi-wms-backend/ 目录下）
mvn clean package -DskipTests

# 2. 启动（在 ruoyi-wms-backend/ 目录下，工作目录影响 SQL 路径解析）
java -jar ruoyi-admin-wms\target\ruoyi-admin-wms.jar
```

启动可能需要 60 秒。启动期间前端访问 `/dev-api/captchaImage` 会返回 502，属正常现象。

配置文件：
- `ruoyi-admin-wms/src/main/resources/application.yml` — 主配置（端口、Sa-Token、MyBatis-Plus、Swagger）
- `ruoyi-admin-wms/src/main/resources/application-dev.yml` — 开发环境（数据源、Redis、RCS 地址）

### 2.3 前端启动

```powershell
# 在 ruoyi-wms-frontend/ 目录下
npm install   # 首次需要
npm run dev   # 启动 Vite dev server, 端口 80
```

前端代理配置（`vite.config.js`）：
```javascript
proxy: {
  '/dev-api': {
    target: 'http://localhost:8080',  // 本机后端
    changeOrigin: true,
    rewrite: (p) => p.replace(/^\/dev-api/, '')
  }
}
```

线上环境代理目标为 `https://wms.ichengle.top`，前端 API 前缀改为 `/prod-api`。

### 2.4 移动端启动

移动端是独立的 HTML 文件 `wms-mobile.html`，通过端口 8000 的 HTTP 服务提供：

```powershell
# 在项目根目录下
python -m http.server 8000
# 访问 http://localhost:8000/wms-mobile.html
```

摄像头扫码功能需要 HTTPS 或 localhost 环境，直接用 IP 访问（如 `http://192.168.1.x:8000`）可能被浏览器阻止。

### 2.5 Git 版本管理

Git 不在系统 PATH 中，使用项目内置的 mingit：

```
.tools\mingit\cmd\git.exe
```

也可使用图形化工具 `git管理.bat`（双击运行），支持查看历史、创建备份点、回滚版本。

当前仓库为本地仓库，未配置远程仓库（`git remote -v` 为空）。

### 2.6 数据库初始化

仅管理员可访问（`@SaCheckRole("admin")`），路径：系统管理 > 数据库初始化。

- `GET /system/database/status` — 查询数据库状态（表数量、数据量）
- `POST /system/database/init` — 执行初始化（请求体须包含 `confirm: "确认初始化"`）

限流：3 次/小时。执行后会按顺序运行 `script/sql/` 下的 SQL 脚本，清空业务数据并重置为初始状态，最后清除 Redis 缓存。

---

## 三、硬约束红线

以下规则不可违背，违反会导致系统功能异常或数据错误。

### 3.1 端口与配置

- PC 端服务端口固定 80，移动端服务端口固定 8000，不得更改
- 前端代理必须与后端地址匹配：本地 `localhost:8080`，线上 `https://wms.ichengle.top`
- 登录接口仅支持 POST，直接 GET 访问会被拒绝
- RCS 系统地址在 `application-dev.yml` 中配置为 `http://192.168.1.36:9001`

### 3.2 容器号规则

- 格式为 5 位零填充递增数字：`00001`、`00002`、`00003`...
- 生成逻辑：查询 `wms_location`、`wms_receipt_order_detail`、`wms_shipment_order_detail`、`wms_movement_order_detail` 四张表中最大的 container_no，加 1 格式化为 5 位
- `generateContainerNo()` 方法加了 `synchronized` 防止并发生成重复号
- 容器号必须在入库/出库操作时自动填充，RCS 任务下发时 barCode（箱号）绝不能为空

容器号三层保障机制：
1. 入库/出库保存前：`ReceiptOrderService.receive()` 和 `ShipmentOrderService.shipment()` 检查 containerNo 是否为空，空则自动生成
2. RCS 下发前：`RcsTaskHelper.buildTaskItem()` 作为最终检查，空则自动补全
3. 库位表 `wms_location.container_no`：入库完成时设置（目标库位），出库完成时清除（源库位）

### 3.3 库位状态机

- 一个折叠库位同一时间只支持一个任务，当前任务完成后才能下发下一个
- 库位区域：A 区（A1-A10 存储）、R 区（R1-R2 入库起点）、C 区（C1-C2 出库终点）
- 自动选库位规则：从 A1-A10 中选择编号最小的空位
- 出库拆分规则：优先从最小库位编号开始扣减库存
- 库位状态：0=空位，1=有货

### 3.4 单据状态流转

- 入库/出库单提交后状态为 `IN_PROGRESS(2)`（入库中/出库中），列表页显示橙色标签
- AGV 回调确认所有任务完成后，状态变为 `FINISH(1)`（已完成），列表页显示绿色标签
- 状态为 IN_PROGRESS(2) 时可撤销，撤销后回退为草稿状态，逆转库存变更、恢复库位状态、取消 RCS 任务
- `markAsFinished(Long id)` 方法用 `LambdaUpdateWrapper` 更新状态为 FINISH(1)

### 3.5 数据库初始化约束

- 仅管理员可访问：后端 `@SaCheckRole("admin")`，前端 `v-hasRole=["admin"]`
- 限流：3 次/小时（`@RateLimiter` 注解）
- 必须显式确认：请求体须包含 `confirm: "确认初始化"`
- 执行后清除 Redis 缓存

### 3.6 版本控制约束

- 所有代码变更必须用 Git 版本控制
- 变更必须逐个增量实施（一次一个改动），每次改动前创建 Git 备份点
- 禁止批量执行多个 AI 建议的改动而不做版本控制（会导致系统不稳定和技术债务）

---

## 四、关键业务流程

### 4.1 入库流程（半延迟方案）

当前采用"半延迟"方案——提交时立即更新库位状态，AGV 回调后再变更库存数量。

```
1. 创建入库单 → 保存单据 + 明细
2. 提交入库（receive()）
   ├─ 生成/补全 containerNo（三层保障第 1 层）
   ├─ 更新目标库位状态为 1（有货），设置 container_no
   ├─ 单据状态设为 IN_PROGRESS(2)
   └─ 下发 RCS 任务（仅下发序列号为 1 的第一个任务）
3. AGV 执行任务，RCS 回调
   ├─ taskStatus=0：任务开始（不是完成）
   ├─ 回调累计 >= 3 条时触发：
   │   ├─ onTaskCompleted()：变更库存数量 + 记录库存流水
   │   ├─ 检查是否有下一个任务（hasNextTask）
   │   │   ├─ 有：下发下一个 RCS 任务
   │   │   └─ 无：markAsFinished() → 单据状态设为 FINISH(1)
   │   └─ 触发下一个暂存入库单（autoReceiveNextPendingOrder）
   └─ 回调日志记录到 wms_agv_log
```

关键方法：
- `RcsController.taskAction()` — AGV 回调入口，回调数 >= 3 时触发后续逻辑
- `RcsController.resolveSourceLocation()` — 从 taskId 解析源库位（解析 orderNo 和 seq）
- `ReceiptOrderService.autoReceiveNextPendingOrder(sourceLocation)` — 按源库位匹配暂存入库单，按创建时间排序处理
- `RcsTaskHelper.dispatchRcsTask()` — 仅下发第一个任务（序列号 1）后立即返回

### 4.2 出库流程

```
1. 创建出库单 → 选择商品（SKU）
2. 系统自动过滤有该 SKU 库存的库位
   └─ 调用 /wms/location/inventoryBySku 接口查询
   └─ 每个明细行存储 sourceLocationOptions 数组
   └─ 无数据时回退到全部有货库位
3. 输入出库数量
   ├─ PC 端"自动拆分"按钮：替换为多行（每个库位一行），从最小库位开始扣减
   └─ 移动端"自动拆分"开关：隐藏源库位和容器号字段
4. 提交出库（shipment()）
   ├─ 补全 containerNo（从源库位获取，回退到入库明细，再回退到自动生成）
   ├─ 更新源库位状态为 0（空位），清除 container_no
   ├─ 单据状态设为 IN_PROGRESS(2)
   └─ 下发 RCS 任务
5. AGV 回调流程同入库
   └─ autoShipNextPendingOrder(targetLocation) 触发下一个暂存出库单
```

### 4.3 AGV 回调时序

RCS 系统会向 WMS 发送两种回调：

**任务状态上报**（兼容路径：`/TaskAction`, `/taskAction`, `/taskStatus`, `/TaskAction/TaskAction`, `/taskAction/taskAction`）：
- `taskStatus=0`：任务开始执行（不是完成）
- 回调累计 >= 3 条时触发库存变更和下一任务下发

**安全信号**（兼容路径：`/AgvSign`, `/agvSign`, `/safetySign`, `/AgvSign/AgvSign`, `/agvSign/agvSign`）：
- `actionType=1`：放货
- `actionType=2`：取货

注意：RCS 系统会在配置的回调地址后自动追加接口路径，导致实际请求路径重复（如原路径后追加 `/TaskAction`）。多个兼容路径就是为了处理这个问题。

### 4.4 RCS 对接细节

- RCS 相关类存放在 `com.ruoyi.wms.rcs` 包
- WMS RCS 接口端点定义在 `RcsController`，路径 `/wms/rcs/`
- `RcsClientService.fillTaskDefaults()` 自动填充必填字段：district（默认 `cy`）、containerType（默认 1）、startNode/endNode（空字符串）、fromArea/toArea（0）
- RCS 任务创建返回"保存主任务时异常"是 RCS 内部业务异常，使用合法的 node/container 数据时仍可能出现
- RCS 配置参数在 `application-dev.yml`：timeout(5000ms)、default-district(cy)、default-container-type(1)

### 4.5 盘点流程

- 盘点单更新时会先删除旧明细
- 库存调整从 `check()` 方法移至 `processLoss()`（报损）和 `processProfit()`（报溢）中执行
- 支持报损、报溢、复盘三种工作流

### 4.6 库存统计

- `/wms/location/inventoryBySku` API：按 SKU 查询各库位剩余库存（入库明细减去出库明细），按库位编码排序
- 库存统计页有"查看库位"按钮，弹窗展示该商品在各库位的库存和容器号
- 仪表盘已替换模拟数据为真实 API 数据（`/wms/dashboard/summary`）

---

## 五、工程约定

### 5.1 前端

- 错误消息自定义在 `src/utils/request.js`
- Vite 代理配置在 `ruoyi-wms-frontend/vite.config.js`
- 路由配置在 `src/router/index.js`，动态路由在 `dynamicRoutes` 中注册
- 操作日志状态显示使用 `dict-tag` 字典组件，与数据库字典定义一致
- 异常信息仅在 `status=0`（失败）时显示
- 撤销任务按钮仅在编辑已有单据（`form.id` 存在）时显示在页面底部按钮栏

### 5.2 后端

- API 文档配置在 `application.yml`（swagger-ui section）
- OpenAPI 依赖在 `ruoyi-common-doc/pom.xml` 和父 `pom.xml`
- 需要 `springdoc-openapi-starter-webmvc-ui` 依赖才能访问 Swagger UI，否则报 500
- MyBatis-Plus 配置：雪花算法主键、逻辑删除（1=已删除, 0=未删除）、驼峰映射
- `ruoyi-system` 模块依赖 `ruoyi-common-ratelimiter`

### 5.3 移动端扫码

- 5 个可选扫码引擎：智能切换（默认）、ZXing、BarcodeDetector、QuaggaJS、jsQR
- 智能切换模式顺序：detector → zxing → quagga → jsqr，连续 15 帧失败自动切换
- Canvas 裁剪优化：ZXing/Quagga 只处理取景框区域，jsQR 使用全画面
- 引擎切换入口：设置页面（持久保存到 localStorage）和扫码界面顶部（实时切换）
- 扫码库文件：`quagga.min.js`（93KB）和 `jsQR.js`（257KB）
- 扫码成功触发：振动反馈（`navigator.vibrate`）+ 提示音（2000Hz 方波 + 1200Hz 锯齿波，音量 0.8，持续 0.2 秒）
- 摄像头 UI：全屏取景框，`facingMode: 'environment'` 优先后置摄像头，成功后自动填充条码并触发添加，300ms 后关闭
- 移动端包含手电筒和 2x 变焦按钮

### 5.4 移动端业务功能

- 全局仓库选择器：菜单页设置，localStorage 持久化，入库/出库/库存/移库页面自动同步
- 每日统计：入库单数、出库单数、扫码次数（扫码成功时自增，每日重置），数据来自 `/wms/dashboard/summary`
- 入库扫码页有"自动库位"开关：启用后下拉框禁用，自动选择 A1-A10 最小空位
- 出库页有"自动拆分"开关：启用后隐藏源库位和容器号字段

### 5.5 PC 端业务功能

- 入库编辑页有"自动库位"开关（在"一品一码"旁边）：启用后目标库位下拉框禁用，自动分配 A1-A10 最小空位
- 出库编辑页有"自动拆分"按钮：输入出库数量后替换为多行（每个库位一行）
- 出库选库位按 SKU 过滤：选择商品后自动调用 `/wms/location/inventoryBySku` 查询有该商品库存的库位

### 5.6 安卓 App

- Capacitor 6 包装移动端网页
- `AndroidManifest.xml` 声明 CAMERA 和 INTERNET 权限
- WebChromeClient 自动授予摄像头权限
- 手动构建 APK 可能遇到 AAPT2 权限错误、Android 14 解析包失败、Capacitor 依赖冲突，需使用兼容的 Gradle/AGP 版本组合

### 5.7 安全与限流

关键接口 IP 限流（`@RateLimiter` 注解）：

| 接口 | 限制 |
|------|------|
| `/login` | 5 次/60 秒 |
| `/captchaImage` | 10 次/60 秒 |
| 入库/出库/移库创建 | 20 次/60 秒 |
| 入库/出库/移库执行 | 10 次/60 秒 |
| RCS 任务下发 | 10 次/60 秒 |
| 数据库初始化 | 3 次/小时 |

### 5.8 库位管理

- 后端：`GET /wms/location/listAll`（查询所有库位）、`PUT /wms/location/release`（释放库位）
- 前端：`src/views/system/location/index.vue`，支持按区域/状态筛选
- 释放操作：单个释放、批量释放、释放所有占用库位
- 仅管理员可见，操作记录到操作日志（`@Log` 注解）
- 释放操作将库位状态重置为 0（空位）并清除 container_no，不可逆

---

## 六、当前开发状态

### 6.1 最近 Git 提交（最近 5 条）

| Commit | 日期 | 说明 |
|--------|------|------|
| `a165e4e` | 08-03 | fix: 修复 wms.sql 表结构与代码实体不匹配导致入库/出库数据操作异常 |
| `82e442e` | 08-03 | fix: 修复数据库初始化功能 SQL 文件路径找不到导致静默跳过 |
| `be449c4` | 08-03 | feat: 出库选库位按 SKU 过滤 + 库存统计页查看库位明细 + 仪表盘真实数据 + 盘点/回调 bug 修复 |
| `0620945` | 08-03 | feat: 新增管理员数据库初始化功能 |
| `edbfa91` | 08-02 | fix: sync updated wms-mobile.html to project root for port 8000 server |

更早的提交包括：API 限流、盘点差异处理、波次拣货、消息通知、打印标签、库存快照、库存预警、键盘快捷键、批次管理、批量操作、扫码反馈、Excel 导出、库存预警系统、移动端盘点/移库/库位查询等功能。

### 6.2 已完成功能清单

**核心业务：**
- 入库单管理（创建、编辑、提交、撤销、AGV 回调自动完成）
- 出库单管理（SKU 库位过滤、自动拆分、提交、撤销、AGV 回调自动完成）
- 移库单管理
- 库存盘点（报损、报溢、复盘）
- 库存统计与查询（按 SKU 查看各库位库存）
- 库存记录流水
- 库存预警（最小/最大库存字段，定时任务检查）
- 库存快照

**RCS/AGV 对接：**
- RCS 任务下发（自动填充默认参数）
- AGV 回调处理（任务状态上报 + 安全信号）
- 回调日志记录（wms_agv_log）
- 自动触发下一个暂存单（入库/出库）
- 回调计数 >= 3 触发库存变更

**移动端：**
- 5 引擎扫码（智能切换/ZXing/BarcodeDetector/QuaggaJS/jsQR）
- 入库/出库/移库/盘点扫码作业
- 全局仓库选择器
- 每日统计
- 自动库位 / 自动拆分
- 手电筒 / 变焦
- 振动 + 提示音反馈

**系统管理：**
- 数据库初始化（管理员专用，限流，确认机制）
- 库位管理（释放库位，批量操作）
- API 限流
- 仪表盘（真实数据）
- 数据大屏
- 波次拣货
- 消息通知
- 打印标签
- Excel 导出
- 批次管理（批次号、生产日期、过期日期）
- 键盘快捷键

### 6.3 当前工作区状态

- Git 工作区干净（仅 `会话总结.md` 为未跟踪文件）
- HEAD 指向 `a165e4e`
- 后端 JAR 需要重新编译才能使最新代码生效（上次编译时间可能滞后于代码提交）
- 前端 Vite dev server 开启时自动热更新

---

## 七、踩坑记录

以下问题曾在开发中实际发生，新 AI 需引以为戒。

### 7.1 后端运行旧 JAR 导致新接口 404

**现象：** 前端调用 `/cancel/{id}` 等新接口报 404 或 "No static resource" 错误。
**原因：** Git 代码已更新，但后端运行的是旧版 JAR，未重新编译。
**解决：** 每次代码变更后必须 `mvn clean package -DskipTests` 重新编译，再启动新 JAR。

### 7.2 HTTP→HTTPS 重定向导致 POST 变 GET

**现象：** POST 请求返回 405 Method Not Allowed。
**原因：** HTTP 到 HTTPS 的重定向会将 POST 请求转为 GET。
**解决：** 确保接口直接使用正确的协议，避免重定向。

### 7.3 wms.sql 表结构与实体类不匹配

**现象：** 数据库初始化后，入库/出库报 "数据操作异常，请联系管理员"，日志显示 `Unknown column 'container_no'`。
**原因：** `wms.sql` 是旧版表结构，缺少后续开发新增的列（container_no、source_location、target_location、batch_no 等）和表（wms_location、wms_agv_log）。
**解决：** 已在 `a165e4e` 提交中补全所有缺失列和表。后续新增实体字段时，必须同步更新 `wms.sql`。

### 7.4 数据库初始化 SQL 路径找不到

**现象：** 执行数据库初始化返回"成功"，但数据未被清除。
**原因：** SQL 文件路径解析失败（工作目录不同导致相对路径不匹配），代码只 `log.warn` 后 `return`，不报错。
**解决：** 已在 `82e442e` 提交中改为多路径尝试（`script/sql/`、`ruoyi-wms-backend/script/sql/`、`../script/sql/`），文件未找到时抛异常，并增加 SQL 执行计数验证。

### 7.5 RCS 任务下发失败"料箱不存在"

**现象：** RCS 任务下发返回"料箱不存在"错误。
**原因：** barCode（箱号）为空。
**解决：** 三层保障机制——入库/出库保存前检查、RCS 下发前补全、库位表同步更新。

### 7.6 AGV 回调 taskStatus=0 不是完成

**现象：** AGV 回调后库存未更新，机器人不动。
**原因：** `taskStatus=0` 表示"任务开始"而非"任务完成"，之前的 `dispatchNextTask()` 逻辑在收到 taskStatus=0 时就认为任务完成，是错误的。
**解决：** 改为回调累计 >= 3 条时才触发 `onTaskCompleted()` 执行库存变更。判断条件从 `== 3` 改为 `>= 3` 防止重复回调。

### 7.7 全延迟方案导致 AGV 无法调度

**现象：** 提交入库单后机器人不动。
**原因：** 全延迟方案将库位状态更新和库存数量变更都延迟到 AGV 回调后执行，导致 RCS 系统看不到容器位置无法调度。
**解决：** 改为半延迟方案——提交时立即更新库位状态并下发 RCS 任务，AGV 回调后再变更库存数量和记录流水。

### 7.8 RCS 回调路径重复

**现象：** RCS 回调请求路径重复（如原路径后追加 `/TaskAction`）。
**原因：** RCS 系统会在配置的回调地址后自动追加接口路径。
**解决：** 配置多个兼容路径：`/TaskAction`、`/taskAction`、`/taskStatus`、`/TaskAction/TaskAction`、`/taskAction/taskAction`；安全信号同理。

### 7.9 缺少 springdoc 依赖导致 Swagger 500

**现象：** 访问 `swagger-ui.html` 报 500 "No static resource swagger-ui.html"。
**原因：** 缺少 `springdoc-openapi-starter-webmvc-ui` 依赖。
**解决：** 在 `ruoyi-common-doc/pom.xml` 和父 `pom.xml` 中添加依赖。

### 7.10 并发生成重复容器号

**现象：** 高并发下生成重复的容器号。
**原因：** `generateContainerNo()` 方法无同步控制。
**解决：** 方法加 `synchronized` 关键字。

### 7.11 ItemSkuService.setItemId() 导致 NULL item_id

**现象：** `itemSku/list` API 的 INNER JOIN 查询失败。
**原因：** `setItemId()` 方法之前只在 barcode 为空时设置 itemId，导致 SKU 记录的 item_id 为 NULL。
**解决：** 修正逻辑确保 item_id 始终被设置。

### 7.12 批量改动不带版本控制导致系统不稳定

**现象：** 一次实施多个 AI 建议的改动后系统出现各种问题。
**原因：** 没有逐个增量实施和版本控制。
**解决：** 每次只实施一个改动，改动前创建 Git 备份点，验证通过后再进行下一个。

### 7.13 ZXing hints 配置错误

**现象：** ZXing 解码器初始化失败。
**原因：** hints 设置错误会破坏解码器初始化。
**解决：** 避免错误的格式限制配置。

---

## 八、下一步开发建议

以下建议按优先级排序，源自之前对系统的全面分析（2026-08-03）。

### P0 - 紧急

**1. 添加核心业务表索引**

当前 `wms_receipt_order_detail`、`wms_shipment_order_detail`、`wms_movement_order_detail` 等明细表缺少常用查询字段的索引，在高频入库/出库场景下查询性能会下降。

建议添加索引的字段：
- `order_id`（所有明细表）
- `sku_id`（所有明细表）
- `container_no`（所有明细表 + wms_location）
- `source_location` / `target_location`（所有明细表）
- `location_code`（wms_location）

**2. 启用数据权限隔离**

当前所有用户可见所有仓库的数据。需要启用 RuoYi 框架的数据权限注解（`@DataScope`），实现按仓库隔离数据。

**3. 安全配置加固**

- 修改数据库默认密码（当前 root/root）
- Redis 设置密码
- Sa-Token JWT 密钥更换为随机长字符串（当前是 `abcdefghijklmnopqrstuvwxyz`）
- 生产环境关闭 p6spy SQL 分析

### P1 - 高优先级

**4. 引入数据库版本管理**

使用 Flyway 或 Liquibase 替代手动 SQL 脚本管理，避免表结构与代码实体不同步的问题反复出现。当前 wms.sql 需要手动维护，容易遗漏新增字段。

**5. 添加主数据缓存**

商品（Item）、SKU、仓库、库位等主数据查询频繁但变更少，应缓存到 Redis 减少数据库压力。

**6. 实现实时推送**

当前 AGV 回调状态只能通过刷新页面查看。建议引入 WebSocket 实现实时推送：
- AGV 任务进度实时更新
- 库存预警实时通知
- 入库/出库完成通知

**7. 完善采购/销售订单模块**

当前系统只有入库/出库单，缺少上游的采购订单和销售订单模块，无法形成完整的供应链闭环。

### P2 - 中优先级

**8. 质检管理**

入库前质检流程：质检单创建、质检结果记录（合格/不合格/让步接收）、不合格品隔离库位。

**9. 退货管理**

退货入库流程：退货单创建、退货原因记录、退货质检、退货入库。

**10. 配置远程仓库**

当前 Git 仓库为本地仓库，建议推送到 GitHub/Gitee 等远程平台，防止本地数据丢失。

**11. 前端构建优化**

`ruoyi-wms-frontend/dist/` 目录下有大量历史构建产物，应配置 `.gitignore` 排除，减少仓库体积。

### P3 - 低优先级

**12. AGV 路径优化**

根据库位使用率优化 AGV 取货路径，减少空驶距离。

**13. 移动端 PWA 改造**

将移动端 Web 改造为 PWA，支持离线扫码、添加到主屏幕、推送通知。

**14. 数据分析与报表**

基于库存快照数据生成趋势分析报表：库存周转率、库位利用率、入库/出库趋势图。

---

## 九、关键文件索引

### 后端核心文件

| 文件 | 作用 |
|------|------|
| `ruoyi-admin-wms/.../controller/RcsController.java` | AGV 回调入口，任务状态上报 + 安全信号处理 |
| `ruoyi-admin-wms/.../controller/LocationController.java` | 库位管理 API（listAll, release, inventoryBySku） |
| `ruoyi-admin-wms/.../controller/DatabaseInitController.java` | 数据库初始化 API |
| `ruoyi-admin-wms/.../controller/DashboardController.java` | 仪表盘数据汇总 API |
| `ruoyi-admin-wms/.../service/ReceiptOrderService.java` | 入库业务逻辑（receive, autoReceiveNextPendingOrder, cancelInProgress, markAsFinished） |
| `ruoyi-admin-wms/.../service/ShipmentOrderService.java` | 出库业务逻辑（shipment, autoShipNextPendingOrder, cancelInProgress, markAsFinished） |
| `ruoyi-admin-wms/.../service/MovementOrderService.java` | 移库业务逻辑 |
| `ruoyi-admin-wms/.../service/CheckOrderService.java` | 盘点业务逻辑（check, processLoss, processProfit） |
| `ruoyi-admin-wms/.../service/LocationService.java` | 库位管理（generateContainerNo, listAll, releaseLocations） |
| `ruoyi-admin-wms/.../service/DatabaseInitService.java` | 数据库初始化（多路径 SQL 执行） |
| `ruoyi-admin-wms/.../rcs/RcsClientService.java` | RCS HTTP 调用（fillTaskDefaults） |
| `ruoyi-admin-wms/.../rcs/RcsTaskHelper.java` | RCS 任务构建（dispatchRcsTask, buildTaskItem） |
| `ruoyi-admin-wms/.../domain/entity/BaseOrderDetail.java` | 明细基类（containerNo, sourceLocation, targetLocation） |
| `ruoyi-admin-wms/.../domain/entity/Location.java` | 库位实体 |
| `ruoyi-admin-wms/.../domain/entity/AgvLog.java` | AGV 回调日志实体 |
| `ruoyi-admin-wms/.../mapper/LocationMapper.java` | 库位 Mapper（selectMaxContainerNo, selectContainerNoFromReceiptDetail） |
| `script/sql/wms.sql` | 核心表结构 + 初始数据（必须与实体类同步） |

### 前端核心文件

| 文件 | 作用 |
|------|------|
| `src/views/wms/order/receipt/edit.vue` | 入库编辑页（自动库位开关） |
| `src/views/wms/order/shipment/edit.vue` | 出库编辑页（SKU 库位过滤、自动拆分） |
| `src/views/wms/inventory/statistic.vue` | 库存统计页（查看库位明细） |
| `src/views/system/database/index.vue` | 数据库初始化页面 |
| `src/views/system/location/index.vue` | 库位管理页面（释放库位） |
| `src/views/dashboard/dashboard.vue` | 仪表盘（真实 API 数据） |
| `src/api/wms/location.js` | 库位管理 API 函数 |
| `src/api/system/database.js` | 数据库初始化 API 函数 |
| `src/router/index.js` | 路由配置（dynamicRoutes） |
| `src/utils/request.js` | 前端错误消息自定义 |
| `vite.config.js` | Vite 代理配置 |
| `.env.development` | 开发环境配置 |
| `.env.production` | 生产环境配置 |

### 配置文件

| 文件 | 作用 |
|------|------|
| `ruoyi-admin-wms/src/main/resources/application.yml` | 主配置（端口、Sa-Token、MyBatis-Plus、Swagger） |
| `ruoyi-admin-wms/src/main/resources/application-dev.yml` | 开发环境（数据源、Redis、RCS 地址） |
| `ruoyi-common-doc/pom.xml` | OpenAPI 依赖 |
| `pom.xml`（父） | 依赖版本管理 |

---

## 十、新会话快速上手检查清单

接手后建议按以下顺序确认环境状态：

1. **确认 Git 状态**：`git log --oneline -5` 确认 HEAD 在 `a165e4e`，`git status` 确认工作区干净
2. **确认后端版本**：检查 `ruoyi-admin-wms/target/ruoyi-admin-wms.jar` 的编译时间是否晚于最后一次代码提交。如果旧了，需要 `mvn clean package -DskipTests` 重新编译
3. **确认数据库**：检查 `wms_receipt_order_detail` 表是否有 `container_no` 列（如果没有，说明 wms.sql 未更新或数据库未初始化）
4. **确认 Redis**：`redis-cli ping` 返回 PONG
5. **确认前端**：访问 `http://localhost/` 能打开登录页
6. **确认后端**：访问 `http://localhost:8080/captchaImage` 返回验证码图片
7. **确认移动端**：访问 `http://localhost:8000/wms-mobile.html` 能打开扫码页面
8. **确认 RCS**：如果需要 AGV 作业，确认 `192.168.1.36:9001` 可达

如果以上任何一项不通过，先解决环境问题再开始开发。每次代码变更前创建 Git 备份点，逐个增量实施。
