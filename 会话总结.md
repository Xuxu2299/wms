# WMS 系统本次会话工作总结

> 日期：2026-08-03 ~ 2026-08-04
> 会话涉及 3 个 commit（be449c4、82e442e、a165e4e）

---

## 一、出库选库位按 SKU 过滤 + 库存统计页查看库位明细

**Commit:** `be449c4`

### 1.1 问题背景

- 出库选择商品后，起点库位下拉框显示所有有货库位，而非仅显示有该商品库存的库位
- 库存统计页无法查看商品分布在哪些库位

### 1.2 修改文件

| 文件 | 类型 | 说明 |
|------|------|------|
| `ruoyi-wms-frontend/src/views/wms/order/shipment/edit.vue` | 前端 | 出库页面重构库位过滤逻辑 |
| `ruoyi-wms-frontend/src/views/wms/inventory/statistic.vue` | 前端 | 库存统计页新增库位明细弹窗 |
| `ruoyi-wms-frontend/src/api/wms/dashboard.js` | 前端 | 仪表盘 API 修正 |
| `ruoyi-wms-frontend/src/views/dashboard/charts.vue` | 前端 | 仪表盘替换模拟数据为真实 API 数据 |
| `ruoyi-wms-frontend/src/views/dashboard/dashboard.vue` | 前端 | 数据大屏接入汇总 API |
| `ruoyi-wms-backend/.../RcsController.java` | 后端 | AGV 回调计数判断 `==` 改为 `>=` |
| `ruoyi-wms-backend/.../CheckOrderService.java` | 后端 | 盘点单更新时增加明细删除逻辑；库存调整移至报损/报溢方法 |
| `ruoyi-wms-backend/.../LocationService.java` | 后端 | `generateContainerNo()` 加 `synchronized` 防并发 |

### 1.3 实现方案

**出库库位过滤：**
- 选择商品后，自动调用 `/wms/location/inventoryBySku` 接口查询该 SKU 有库存的库位列表
- 在每个明细行上存储 `sourceLocationOptions` 数组
- 起点库位下拉框优先显示按 SKU 过滤的库位，无数据时回退到全部有货库位

**库存统计页库位明细：**
- 表格新增"查看库位"操作按钮
- 点击弹出弹窗，展示该商品在各库位的剩余库存、容器号
- 底部显示库位库存总量汇总

**附带修复：**
- RcsController：AGV 回调计数判断从 `== 3` 改为 `>= 3`，防止重复回调导致库存不更新
- CheckOrderService：盘点单更新时增加明细删除逻辑；库存调整从 `check()` 移至 `processLoss()`/`processProfit()` 中执行
- LocationService：`generateContainerNo()` 加 `synchronized` 防止并发生成重复容器号
- Dashboard：仪表盘和数据大屏替换模拟数据为真实 API 数据

---

## 二、修复数据库初始化功能 SQL 文件路径找不到导致静默跳过

**Commit:** `82e442e`

### 2.1 问题背景

- 管理员执行数据库初始化后，系统返回"成功"，但实际数据未被清除（入库单仍为 72 条，出库单仍为 41 条）

### 2.2 根因分析

| 层面 | 详情 |
|------|------|
| 后端启动方式 | `java -jar ruoyi-admin-wms\target\ruoyi-admin-wms.jar`，工作目录为 `ruoyi-wms-backend/` |
| SQL 路径配置 | `ruoyi-wms-backend/script/sql/wms.sql`（相对于项目根目录） |
| 实际解析路径 | `ruoyi-wms-backend/ruoyi-wms-backend/script/sql/wms.sql`（不存在） |
| classpath 回退 | SQL 文件在 `script/sql/` 目录，不在 `src/main/resources/` 中，classpath 也找不到 |
| 关键缺陷 | 文件找不到时代码只 `log.warn` 后 `return`，不报错，导致初始化返回"成功"但未执行任何 SQL |

### 2.3 修改文件

| 文件 | 类型 | 说明 |
|------|------|------|
| `ruoyi-wms-backend/.../DatabaseInitService.java` | 后端 | 重写 SQL 文件查找逻辑 |
| `ruoyi-wms-frontend/src/views/system/database/index.vue` | 前端 | 结果弹窗增加 SQL 执行条数展示 |

### 2.4 修复方案

- **多路径尝试**：依次尝试 `script/sql/`、`ruoyi-wms-backend/script/sql/`、`../script/sql/` 三个前缀和 classpath
- **文件未找到抛异常**：不再静默跳过，而是抛出异常并记录到 errors 列表
- **SQL 执行计数**：跟踪实际执行的 SQL 语句数，结果中展示 `totalStatements`，若为 0 则标记为失败

### 2.5 验证结果

| 指标 | 修复前 | 修复后 |
|------|--------|--------|
| SQL 执行条数 | 0（静默跳过） | 387 条 |
| 入库单数量 | 72（未变） | 1（重置成功） |
| 出库单数量 | 41（未变） | 1（重置成功） |

---

## 三、修复 wms.sql 表结构与代码实体不匹配导致入库/出库数据操作异常

**Commit:** `a165e4e`

### 3.1 问题背景

- 数据库初始化后，入库/出库操作提示"数据操作异常，请联系管理员"
- 后端日志报错：`Unknown column 'container_no' in 'SELECT'` 和 `Unknown column 'batch_no' in 'SELECT'`

### 3.2 根因分析

数据库初始化用的 `wms.sql` 是旧版表结构，缺少后续开发中新增的列和表：

| 表 | 缺失的列 | 严重程度 |
|----|---------|----------|
| `wms_location` | **整个表不存在**（id, location_code, warehouse_id, area, status, container_no, remark） | 致命 |
| `wms_receipt_order_detail` | `container_no`, `source_location`, `target_location`, `batch_no`, `production_date`, `expiry_date` | 高 |
| `wms_shipment_order_detail` | `container_no`, `source_location`, `target_location` | 高 |
| `wms_movement_order_detail` | `container_no`, `source_location`, `target_location` | 高 |
| `wms_agv_log` | **整个表不存在** | 高 |

### 3.3 修改文件

| 文件 | 类型 | 说明 |
|------|------|------|
| `ruoyi-wms-backend/script/sql/wms.sql` | SQL | 补全缺失列和表定义 |
| `ruoyi-wms-backend/.../LocationController.java` | 后端 | 新增库位管理 API（listAll、release） |
| `ruoyi-wms-backend/.../LocationService.java` | 后端 | 新增 listAll()、releaseLocations() 方法 |
| `ruoyi-wms-frontend/src/api/wms/location.js` | 前端 | 新增 listAllLocations()、releaseLocations() API |
| `ruoyi-wms-frontend/src/router/index.js` | 前端 | 新增库位管理路由 |
| `ruoyi-wms-frontend/src/views/system/location/index.vue` | 前端 | 新增库位管理页面（含释放库位功能） |

### 3.4 修复方案

**SQL 表结构补全：**
- 三个明细表新增 `container_no`、`source_location`、`target_location` 列
- `wms_receipt_order_detail` 额外新增 `batch_no`、`production_date`、`expiry_date` 列
- 新增 `wms_location` 表及 14 个初始库位数据（A1-A10 存储、R1-R2 入库起点、C1-C2 出库终点）
- 新增 `wms_agv_log` 表
- 更新所有 INSERT 语句匹配新列数

**库位管理页面（新增功能）：**
- 后端：`GET /wms/location/listAll`（查询所有库位）、`PUT /wms/location/release`（释放库位）
- 前端：库位管理页面，支持按区域/状态筛选，支持单个释放、批量释放、释放所有占用库位
- 权限控制：仅管理员可访问（`@SaCheckRole("admin")`）
- 操作日志：释放操作记录到操作日志（`@Log` 注解）

### 3.5 验证结果

| 指标 | 结果 |
|------|------|
| SQL 执行条数 | 405 条 |
| `generateContainerNo` API | 正常返回（不再报错） |
| `inventoryBySku` API | 正常返回（不再报错） |
| A1-A10 库位状态 | 全部 status=0（空位） |

---

## 四、Git 提交记录

| Commit | 日期 | 说明 |
|--------|------|------|
| `a165e4e` | 2026-08-03 | fix: 修复 wms.sql 表结构与代码实体不匹配导致入库/出库数据操作异常 |
| `82e442e` | 2026-08-03 | fix: 修复数据库初始化功能 SQL 文件路径找不到导致静默跳过的问题 |
| `be449c4` | 2026-08-03 | feat: 出库选库位按 SKU 过滤 + 库存统计页查看库位明细 + 仪表盘真实数据 + 盘点/回调 bug 修复 |

---

## 五、变更文件清单

### 后端文件（6 个）

1. `ruoyi-wms-backend/script/sql/wms.sql` — 补全表结构和初始数据
2. `ruoyi-wms-backend/.../controller/LocationController.java` — 新增库位管理 API
3. `ruoyi-wms-backend/.../service/LocationService.java` — 新增 listAll()、releaseLocations() 方法
4. `ruoyi-wms-backend/.../controller/RcsController.java` — AGV 回调计数修复
5. `ruoyi-wms-backend/.../service/CheckOrderService.java` — 盘点逻辑修复
6. `ruoyi-wms-backend/.../service/DatabaseInitService.java` — SQL 文件路径查找修复

### 前端文件（6 个）

1. `ruoyi-wms-frontend/src/views/wms/order/shipment/edit.vue` — 出库库位按 SKU 过滤
2. `ruoyi-wms-frontend/src/views/wms/inventory/statistic.vue` — 库存统计页库位明细
3. `ruoyi-wms-frontend/src/views/system/database/index.vue` — 初始化结果展示优化
4. `ruoyi-wms-frontend/src/views/system/location/index.vue` — **新增** 库位管理页面
5. `ruoyi-wms-frontend/src/api/wms/location.js` — 新增库位管理 API 函数
6. `ruoyi-wms-frontend/src/router/index.js` — 新增库位管理路由

---

## 六、注意事项

1. **数据库初始化会清空所有业务数据**，仅保留初始菜单、角色、用户、商品、仓库等基础数据
2. **库位管理页面**仅管理员可见，路径为 系统管理 > 库位管理
3. **释放库位**操作不可逆，会将库位状态重置为空位并清除容器号
4. 修复后的 `wms.sql` 包含 `wms_location` 表和 14 个初始库位（A1-A10、R1-R2、C1-C2），全部为空位状态
5. 后端需要重新编译才能使新增的库位管理 API 生效
